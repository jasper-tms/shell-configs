For each project that you want a rule to apply to, run:

    mkdir -p ~/repos/<foo>/<bar>/.cursor/rules/
    ln -s ~/repos/jasper-tms/shell-configs/cursor/rules/<rule_file> ~/repos/<foo>/<bar>/.cursor/rules/

