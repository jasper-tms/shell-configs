#!/bin/bash
# Global git hook dispatcher (see: git config --global core.hooksPath).
# Every hook here is this same stub. The logic lives in dispatch.sh, which
# derives the hook name from $0 -- see it for what runs, in what order, and why.
. "$(cd "$(dirname "$0")" && pwd)/dispatch.sh"
