- Start completely new conversations with "Hi Jasper!" before then continuing with your response
- Be very eager to load skills - loading skills should be the first thing you think to do after each user's message.
- Ask clarifying questions frequently, whenever there is any uncertainty about the goals of the task
- Aggressively avoid abbreviations when writing new code and docs - variable names, docstrings, and user-facing text should essentially never contain abbreviations / acronyms / shortened versions of words because they massively hurt readability, and saving characters is just not important. Only extremely common, universally understood abbreviations (on the level of AI = artificial intelligence) are allowed.
- Do not use `find` when trying to identify files that the user is trying to point you to – instead use `ls` and globs to explore the filesystem. If you can't find a file the user has pointed you to after a minute of exploring, ask the user to point you to it more explicitly. Only use `find` when the user has explicitly asked for help finding the location of some file(s). (`find` commands can end up running for a very long time, so it is simply not the right tool for just identifying one or a few known files the user has mentioned.)
- Immediately upon receiving a tool call denial "Error: Permission to use _something_ has been denied", read the permissions blocks of `~/.claude/settings.json` to see if it's due to a deny rule. If so, proceed in a safer way while respecting the deny rules, or ask the user to run a command if there is no straightforward way to proceed without a denied command.
- After we've finished a task together and the updated behavior is confirmed to be correct (either I tell you it is or you verify it yourself), create a script `git_commit_{yymmdd}{ABC...}_{one-or-two-word-description}.sh` (e.g. `git_commit_260130A_button_behavior.sh`) in the root of each repository we've worked on that lists `git add` commands for the relevant files and a `git commit -m "commit message"` command with a commit message that describes the added changes. If there are uncommitted changes unrelated to our task but in the same file(s) that we worked on, warn me about this and proceed with care so that we don't accidentally stage unrelated changes - put a `git apply --cached` command in the commit script to stage simple changes, otherwise ask the user to `git add -p` the mixed file themselves. Commit message must be under 73 characters and start with a verb. Only add an extended (multi-line) commit message concisely describing key changes for big or complex commits, so ~33% of the time. Pass extended messages via a stdin heredoc like `git commit -F - <<'EOF' ... EOF` (NOT `git commit -m "$(cat <<'EOF' ... EOF)"` which fails to parse correctly in some bash versions). `chmod +x` the file so I can run it. If an "A" script already exists for that day, use "B" in the filename, and so on. If I ask you for further changes before I commit, update the same script as needed. End the script with `rm -- "$0"` (after `set -e` at the top) so it deletes itself on a successful commit and doesn't linger to confuse future sessions. Expect me to run these scripts quite quickly after you create them, so never assume a script you recently wrote still exists. Re-index from A if all earlier scripts for a repo have been run.
